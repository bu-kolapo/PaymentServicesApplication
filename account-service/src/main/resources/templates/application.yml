server:
  port: 8081

spring:
  application:
    name: account-service

  data:
    mongodb:
      database: accountdb
      host: localhost
      port: 27017

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
