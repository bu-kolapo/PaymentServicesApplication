server:
  port: 8083

spring:
  application:
    name: transaction-service

  data:
    mongodb:
      database: transactiondb
      host: localhost
      port: 27017

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
