server:
  port: 8082

spring:
  application:
    name: customer-service

  data:
    mongodb:
      database: customerdb
      host: localhost
      port: 27017

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
